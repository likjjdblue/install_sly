apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webcollection-pvc-log
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: webcollection-pv-log-class
  resources:
     requests:
       storage: 200Gi
