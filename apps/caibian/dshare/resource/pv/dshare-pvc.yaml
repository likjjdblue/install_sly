apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dshare-pvc-log
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: dshare-pv-log-class
  resources:
     requests:
       storage: 1000Gi
