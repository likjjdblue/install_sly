apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperreview-pvc-log
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: paperreview-pv-log-class
  resources:
     requests:
       storage: 1000Gi
